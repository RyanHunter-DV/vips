## rhload "#{ENV['UVMHOME']}/root"
## rhload "#{ENV['RHUVMDBG']}/root"
## rhload "#{ENV['RHLIB']}/root"
rhload 'src/node'
rhload 'examples/node'


config :compileConfig do
	component :RhGpVip
	component :examples
end

test :sanityTest do
	config :compileConfig
	simopt :xlm,'+UVM_TESTNAME=baseTest'
	simopt :xlm,'+UVM_TIMEOUT=1us'
end

context :selfTest do
	component :RhGpVip
	component :examples

	config :compileConfig
	test :sanityTest
end
